const knex = require('./dbConnection');

module.exports = {

    // getIdforUser: (param) =>{
    //     console.log(param);
    //     return knex.select(id).from('users').where({username: param})
        
    // }

    
}